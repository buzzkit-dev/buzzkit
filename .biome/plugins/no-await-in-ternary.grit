language js

`$condition ? $consequence : $alternative` where {
  or { $consequence <: contains `await $_`, $alternative <: contains `await $_` },
  register_diagnostic(span=$condition, message="Ternaries never wrap awaited calls — branch with if/early-return instead (see the conventions skill).")
}
