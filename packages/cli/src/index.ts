// @buzzkit/cli — pushes code-defined campaigns, workflows, and segments to a
// buzzkit deployment (in the spirit of `sst deploy`). Placeholder until the core exists.

export { version } from 'buzzkit';
